import{t as e}from"./site-config.BegNMtY8.js";import{t}from"./draft-format.CLABUIXs.js";import{n,t as r}from"./liff-init.CfdR1rZp.js";var i=document.getElementById(`whoami`),a=document.getElementById(`list`),o=null;async function s(t,n){let r=await fetch(`${e.workerBase}/approval/${t}`,{method:`POST`,headers:{"content-type":`application/json`},body:JSON.stringify({line_id_token:o,...n})});return{ok:r.ok,data:await r.json()}}async function c(){let{ok:e,data:n}=await s(`mine`,{});if(!e){a.innerHTML=`<p class="muted">讀取失敗：${n.error}</p>`;return}if(!n.bound){a.innerHTML=`<p class="muted">你還沒報到綁定職位——請點管理員給你的專屬邀請連結完成報到。</p>`;return}if(!n.cases.length){a.innerHTML=`<p class="muted">目前沒有輪到你的案件 ✅</p>`;return}a.innerHTML=n.cases.map(e=>{let n=t(e.cpc_versions.content),r=n.structured?[n.title&&`<h4>${n.title}</h4>`,...n.body.map(e=>`<p>${e}</p>`),n.quote&&`<blockquote>${n.quote}</blockquote>`].filter(Boolean).join(``):`<p>${e.cpc_versions.content.replace(/\n/g,`<br>`)}</p>`;return`<article class="case" data-id="${e.id}">
          <header><strong>${e.cpc_versions.title}</strong> <span class="muted">v${e.cpc_versions.version_no}｜${e.cpc_templates.name}</span></header>
          <div class="draft">${r}</div>
          <textarea class="comment" placeholder="簽註（選填；退回時建議說明原因）" rows="2"></textarea>
          <div class="actions">
            <button type="button" class="approve">✅ 核准</button>
            <button type="button" class="reject">⛔ 退回</button>
          </div>
        </article>`}).join(``),a.querySelectorAll(`article.case`).forEach(e=>{let t=async t=>{let n=e.querySelector(`.comment`).value;e.querySelectorAll(`button`).forEach(e=>e.disabled=!0);let{ok:r,data:i}=await s(`act`,{case_id:e.dataset.id,action:t,comment:n});e.querySelector(`.actions`).innerHTML=r?`<span>${t===`approve`?`✅ 已核准`:`⛔ 已退回`}${i.result===`approved_final`?`（全案決行）`:``}</span>`:`<span class="muted">失敗：${i.error}</span>`};e.querySelector(`.approve`).addEventListener(`click`,()=>t(`approve`)),e.querySelector(`.reject`).addEventListener(`click`,()=>t(`reject`))})}r().then(async e=>{if(!e?.idToken){i.innerHTML=`需要 LINE 身份：<button type="button" id="loginBtn">用 LINE 登入</button>`,document.getElementById(`loginBtn`).addEventListener(`click`,()=>n());return}o=e.idToken,i.textContent=`LINE：${e.displayName}`;let t=new URLSearchParams(location.search).get(`bind`);if(t){let{ok:e,data:n}=await s(`bind`,{token:t}),r=document.getElementById(`bindResult`);r.hidden=!1,r.innerHTML=e?`<p class="bind-ok">🎉 報到完成：<strong>${n.bound}</strong>。輪到你的案件會推播到 LINE。</p>`:`<p class="muted">報到失敗：${n.error===`token_used`?`這個連結已被使用`:n.error}</p>`,history.replaceState(null,``,location.pathname)}c()});